export interface formFieldsTypes {
    id: number;
    type: string;
    htmlFor: string;
    label: string;
    placeholder: string;
}

export interface passwordStatusesTypes {
    [key: string]: boolean;
}
